---
- name: Converge
  hosts: all
  become: true

  pre_tasks:
    - name: Update apt cache.
      apt:
        update_cache: yes
      when: (ansible_os_family | lower) == 'debian'

  roles:
    - role: mlaurent205.ansible-role-passbolt
